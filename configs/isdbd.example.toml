http_port = 8010
storage_root = "/var/lib/isdbd"
adapters = [0]

dvbr_bin     = "/home/duckfeather/code/isdb-workspace/dvbr/target/release/dvbr"
b25_bin      = "/home/duckfeather/code/isdb-workspace/libaribb25-rs/target/release/b25"
ffmpeg_bin   = "ffmpeg"

channels_file = "channels.json"

# EPG refresh cron (UTC). Default: every 6 hours.
epg_cron = "0 */6 * * *"
epg_channels = ["TOKYO MX1", "NHKEFl1El5~", "asahi"]
